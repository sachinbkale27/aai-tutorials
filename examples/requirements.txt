# Deps used across the runnable examples (install only what a given example needs).
openai>=1.40
langgraph>=0.2
langchain-core>=0.3
langchain-openai>=0.2
mcp>=1.2
chromadb>=0.5
tenacity>=8.0
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp>=1.27
nemoguardrails==0.16.0
fastapi>=0.115
uvicorn[standard]>=0.30
pyyaml>=6.0
pytest>=8.0
# RAGAS (example 15) — version-sensitive; install in a CLEAN venv (conflicts with pinned langchain/nemoguardrails):
#   pip install "ragas==0.2.10" "langchain-community==0.3.14" "langchain-openai>=0.2"
#   (newer langchain-community removes langchain_community.chat_models.vertexai, which ragas imports)
